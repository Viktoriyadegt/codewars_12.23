//========================1 way==================================
function reverseSeq(n) {
    if (n < 1) {
      return [];
    } else {
      const countArray = reverseSeq(n - 1);
      countArray.unshift(n);
      return countArray;
    }
  }
  console.log(reverseSeq(5));

  