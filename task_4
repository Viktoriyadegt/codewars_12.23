function multiTable(number) {
    let string = []
    for(let i = 1; i<=10; i++){
            string. push(`${i} * ${number} = ${i*number}`) 
  }
  return string.join('\n')
}
console.log(multiTable(5))